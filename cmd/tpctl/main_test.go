package main

// The binary is exercised via the internal/cli package's tests and the
// e2e tests under cmd/tpctl. Keeping this file as a placeholder so the
// package always has at least one compilable Go file in test mode.
